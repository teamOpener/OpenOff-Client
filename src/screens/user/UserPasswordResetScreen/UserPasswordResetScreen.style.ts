import { StyleSheet } from 'react-native';

const userPasswordResetScreenStyles = StyleSheet.create({
  passwordResetTitle: {
    width: '100%',
    alignItems: 'center',
    paddingVertical: 40,
  },
});

export default userPasswordResetScreenStyles;
