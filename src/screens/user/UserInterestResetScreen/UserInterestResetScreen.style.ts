import { StyleSheet } from 'react-native';

const userInterestResetScreenStyles = StyleSheet.create({
  fieldInfoImage: {
    marginTop: -26,
    width: 198,
    height: 37,
  },
});

export default userInterestResetScreenStyles;
