import { StyleSheet } from 'react-native';

const emailFindScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 40,
  },
  authorizeContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  checkImage: {
    width: 100,
    height: 100,
    marginBottom: 20,
  },
});

export default emailFindScreenStyles;
