import { StyleSheet } from 'react-native';

const passwordResetCompleteScreenStyles = StyleSheet.create({
  authorizeContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  checkImage: {
    width: 100,
    height: 100,
    marginBottom: 20,
  },
});

export default passwordResetCompleteScreenStyles;
