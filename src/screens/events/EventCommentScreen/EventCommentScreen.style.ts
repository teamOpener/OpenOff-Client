import { StyleSheet } from 'react-native';
import { layouts } from 'styles/theme';

const eventCommentScreenStyles = StyleSheet.create({
  scrollContainer: {
    flex: 1,
    paddingTop: layouts.PADDING,
  },
});

export default eventCommentScreenStyles;
