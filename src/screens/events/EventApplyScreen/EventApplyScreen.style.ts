import { StyleSheet, TextStyle } from 'react-native';
import { fonts } from 'styles/theme';

const eventApplyScreenStyles = StyleSheet.create({
  subTitle: {
    fontFamily: fonts.semibold,
    fontSize: 17,
    lineHeight: 23.8,
  } as TextStyle,
});

export default eventApplyScreenStyles;
