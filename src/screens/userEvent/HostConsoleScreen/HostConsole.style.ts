import { StyleSheet } from 'react-native';

const hostConsoleStyles = StyleSheet.create({
  chartContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    alignSelf: 'center',
  },
  buttonContainer: {
    paddingHorizontal: 20,
  },
});

export default hostConsoleStyles;
