import { StyleSheet } from 'react-native';

const userTicketScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    gap: 10,
  },
  carousel: {
    justifyContent: 'center',
  },
});

export default userTicketScreenStyles;
