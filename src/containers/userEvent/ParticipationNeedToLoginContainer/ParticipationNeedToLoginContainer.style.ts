import { StyleSheet } from 'react-native';

const participationNeedToLoginContainerStyles = StyleSheet.create({
  container: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
});

export default participationNeedToLoginContainerStyles;
