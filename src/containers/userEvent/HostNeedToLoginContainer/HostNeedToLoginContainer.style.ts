import { StyleSheet } from 'react-native';

const hostNeedToLoginContainerStyles = StyleSheet.create({
  container: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
});

export default hostNeedToLoginContainerStyles;
