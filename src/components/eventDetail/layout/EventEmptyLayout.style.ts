import { StyleSheet } from 'react-native';

const eventEmptyLayoutStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    marginBottom: 20,
    alignItems: 'center',
  },
});

export default eventEmptyLayoutStyles;
