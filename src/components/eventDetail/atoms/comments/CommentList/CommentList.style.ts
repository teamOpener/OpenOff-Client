import { StyleSheet, TextStyle } from 'react-native';

const commentListStyles = StyleSheet.create({
  emptyContainer: { paddingVertical: 20 },
  emptyText: { textAlign: 'center' } as TextStyle,
});

export default commentListStyles;
