import { StyleSheet } from 'react-native';

const carouselCardSkeletonStyles = StyleSheet.create({
  container: {
    backgroundColor: 'floralwhite',
    borderRadius: 13,
    marginRight: 20,
    height: 237,
    overflow: 'hidden',
  },
});

export default carouselCardSkeletonStyles;
