import { StyleSheet } from 'react-native';

const commentRowSkeletonStyles = StyleSheet.create({
  title: { width: 90, height: 14, borderRadius: 2 },
  description: { width: 120, height: 12, borderRadius: 2, marginTop: 12 },
});

export default commentRowSkeletonStyles;
