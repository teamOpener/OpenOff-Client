import { StyleSheet } from 'react-native';
import { colors } from 'styles/theme';

const authorizeFlowTitleStyles = StyleSheet.create({
  title: {
    color: colors.white,
    width: '100%',
  },
  titleContainer: {
    marginBottom: 40,
    marginTop: 20,
  },
});

export default authorizeFlowTitleStyles;
