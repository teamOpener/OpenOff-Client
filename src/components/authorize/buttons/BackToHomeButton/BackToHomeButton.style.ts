import { StyleSheet } from 'react-native';

const backToHomeButtonStyles = StyleSheet.create({
  container: {
    paddingHorizontal: 15,
    justifyContent: 'center',
  },
});

export default backToHomeButtonStyles;
