import { StyleSheet } from 'react-native';

const selectDetailBoxStyles = StyleSheet.create({
  container: {
    flexDirection: 'column',
  },
  selectBoxContainer: {
    width: 300,
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
});

export default selectDetailBoxStyles;
