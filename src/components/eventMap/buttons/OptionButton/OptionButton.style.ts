import { StyleSheet } from 'react-native';

const optionButtonStyles = StyleSheet.create({
  container: {
    height: 28,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default optionButtonStyles;
