import { StyleSheet } from 'react-native';

const selectDetailButtonStyles = StyleSheet.create({
  container: {
    borderRadius: 17,
    borderWidth: 1,
    paddingHorizontal: 17,
    paddingVertical: 8,
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: 5,
    marginTop: 10,
  },
});

export default selectDetailButtonStyles;
