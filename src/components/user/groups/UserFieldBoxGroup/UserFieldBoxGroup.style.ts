import { StyleSheet } from 'react-native';

const userFieldBoxGroupStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: 10,
  },
});

export default userFieldBoxGroupStyles;
