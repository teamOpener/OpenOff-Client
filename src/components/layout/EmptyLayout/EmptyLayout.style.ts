import { StyleSheet } from 'react-native';

const emptyLayoutStyles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: '68%',
    alignItems: 'center',
  },
});

export default emptyLayoutStyles;
