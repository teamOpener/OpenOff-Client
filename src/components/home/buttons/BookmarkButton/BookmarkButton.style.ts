import { StyleSheet } from 'react-native';

const bookmarkButtonStyles = StyleSheet.create({
  bookmarkButton: {
    justifyContent: 'center',
    alignItems: 'center',
    position: 'absolute',
    bottom: 5,
    right: 5,
  },
});

export default bookmarkButtonStyles;
