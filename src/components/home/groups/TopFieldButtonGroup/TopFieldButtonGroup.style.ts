import { StyleSheet } from 'react-native';

const topFieldButtonGroupStyles = StyleSheet.create({
  fieldContainer: {
    flexDirection: 'row',
    paddingTop: 18,
    marginBottom: 20,
  },
});

export default topFieldButtonGroupStyles;
