import { StyleSheet } from 'react-native';

const eventRowCardListStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  flatListcontainer: {
    flex: 1,
  },
});

export default eventRowCardListStyles;
