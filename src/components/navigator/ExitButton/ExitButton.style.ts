import { StyleSheet } from 'react-native';

const exitButtonStyles = StyleSheet.create({
  container: {
    paddingHorizontal: 15,
    justifyContent: 'center',
  },
});

export default exitButtonStyles;
