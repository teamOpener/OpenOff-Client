import { StyleSheet } from 'react-native';

const costStyles = StyleSheet.create({
  horizontalView: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 5,
  },
  input: {
    flex: 1,
  },
});

export default costStyles;
