import { StyleSheet } from 'react-native';

const periodStyles = StyleSheet.create({
  horizontalScrollView: {
    columnGap: 14,
    paddingTop: 5,
  },
  inputContainer: {
    gap: 5,
  },
});

export default periodStyles;
