import { StyleSheet } from 'react-native';

const recruitmentNumberStyles = StyleSheet.create({
  input: {
    width: '30%',
  },
  inputWithHelpText: {
    gap: 5,
  },
});

export default recruitmentNumberStyles;
